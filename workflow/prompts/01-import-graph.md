Import the existing thematic graph.

Preserve each node's ID, label, description, and examples. Edges from the original graph represent only the thematic or hierarchical relationships defined by that graph; they do not automatically imply causality.

Only import the graph at this stage. Do not generate new node relationships, theories, or predictions.
