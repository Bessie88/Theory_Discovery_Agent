Conduct Corbin & Strauss open coding on this batch only. Code meaningful
segments, not mechanically every sentence. A segment may have multiple codes
only for distinct phenomena. Mark every supplied record as processed, even when
it produces no update.

Compare every update with the live inventory:

- `SAME`: reuse the concept and add verbatim evidence.
- `VARIATION`: preserve an evidence-grounded conditional form.
- `NEW`: use only for a genuinely distinct phenomenon; give a concise,
  corpus-specific label and definition.
- `CONTRADICTION`: preserve boundary or opposing evidence; do not average it
  away.

Use `revised_definition` only for a material evidence-grounded refinement, not
wordsmithing. Do not create codes for superficial wording changes, import old
themes or external knowledge, or infer relationships. Every evidence span must
be an exact substring of its record. Write memos only for an analytic change,
variation, contradiction, or unresolved question.

Return JSON only, following `expected_output`.
