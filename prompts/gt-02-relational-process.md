Conduct Corbin & Strauss relational and process analysis on this batch against
the live inventories. Examine conditions, actions/interactions, consequences,
and pathways only where the records support them; not every concept needs a
relationship.

Never treat co-occurrence as a relationship. Do not turn two concepts in one
record, two unrelated cases, or an indirect A → B → C sequence into a direct
arrow. Every relationship needs verbatim evidence, a comparative basis, and:

- `explicitly_expressed` when the wording directly states the link;
- `repeated_comparison` when at least two records support the same link;
- `tentative_theoretical_inference` only for a clearly tentative cross-record
  inference with at least two records.

State why in `grounding_explanation`. Use `SAME`, `VARIATION`, `NEW`, or
`CONTRADICTION` against the existing relation/process inventory; preserve
negative cases and conditions rather than averaging them away.

Stage 2 may revise the concept/category inventory when this batch warrants it.
Make the `concept_updates` explicit, evidence-grounded, and verbatim: refine a
definition, change a supported category placement, create a genuinely new
category, or record a variation/boundary case. Do not silently rename or
reinterpret Stage 1.

Use natural-language relation phrases. A process needs explicit `edges`; every
edge must exactly name its source, phrase, target, and direct existing relation
ID(s) or relationship-update index(es). A description, record list, or chain is
not evidence for an arrow. Each edge will be reviewed separately. Write memos
only for material analytic developments.

Return JSON only, following `expected_output`.
