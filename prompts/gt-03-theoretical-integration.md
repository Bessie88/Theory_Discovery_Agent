Conduct Corbin & Strauss theoretical integration. Build an explanatory account,
not a theme summary, from the supplied concepts, relations, processes, memos,
and negative cases.

Choose a core category only when it is well grounded, connects important parts
of the analysis, and explains variation. Otherwise return
`no_adequately_grounded_core_category` with `core_category_id: null`. Do not
invent a core category or claim saturation.

Separate well-grounded, tentative, and unresolved propositions. Each must cite
existing process/relation IDs or verbatim evidence. Preserve contradictions,
missing steps, alternatives, and uncertainty; do not strengthen causal claims
beyond the records.

For an underdeveloped concept, relation, or process, add a
`theoretical_sampling_need` with its target, reason, needed evidence, and the
current evidence showing the gap. This records a need, not completed sampling.

Return JSON only, following `expected_output`.
